const { expect } = require('chai');

describe('Ao chamar o controller de create - controller', () => {
  describe('quando o payload informado não é válido', () => {
    it('é chamado o status com o código 422', () => {

    });

    it('é chamado o json com a mensagem "Dados inválidos"', () => {

    });
  });

  describe('quando é inserido com sucesso', () => {
    it('é chamado o status com o código 201', () => {

    });

    it('é chamado o json com a mensagem "Filme criado com sucesso!"', () => {

    });
  });
});