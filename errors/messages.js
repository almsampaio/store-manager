const invaliData = 'invalid_data';

const codes = {
  invaliData: 'invalid_data',
};

const messages = {
  productAlreadyExists: 'Product already exists',
  productNameLength: '"name" length must be at least 5 characters long',
};

module.exports = {
  invaliData,
  codes,
  messages,
};
