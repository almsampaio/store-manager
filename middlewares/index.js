const nameAuth = require('./nameAuth');
const quantityAuth = require('./quantityAuth');
const salesQuantityAuth = require('./salesQuantityAuth');
const handleError = require('./handleError');

module.exports = {
  nameAuth,
  quantityAuth,
  salesQuantityAuth,
  handleError,
};
